[package]
name = "matching-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
ordered-float = "4"
thiserror = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"

[[bench]]
name = "orderbook_bench"
harness = false
