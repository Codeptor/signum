# Claude Code Configuration

## Commands
- **Always use `uv`** to run Python: `uv run pytest`, `uv run python`, etc.
- Never use `.venv/bin/python`, `pip`, or bare `python`

## Git
- Do NOT include `Co-Authored-By` in commit messages
